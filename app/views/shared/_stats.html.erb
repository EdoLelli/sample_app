<% @user ||= current_user %>
<div class="stats">
	 <a href="<%= user_path(@user) %>">
    <strong id="microposts" class="stat"><%=@user.microposts.count%></strong>
     POST
  </a>
  <a href="<%= following_user_path(@user) %>">
    <strong id="following" class="stat"><%= @user.followed_users.count %></strong>
    FOLLOWING
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stat"><%= @user.followers.count %></strong>
    FOLLOWER
  </a>
</div>

